# Build Record — `clinic_ap` 19.0.3.0.2

## Baseline observation
The active non-backup `clinic_ap` baseline contained 24 files. Python already contained meaningful AP, aging, cashflow, purchase/inventory, Billing and accounting logic, while active UI/security remained mostly scaffold/minimal.

## Full-build decisions
- preserved meaningful AP engines instead of replacing them with a simplified model;
- repaired the missing `clinic.ap.line.stock_move_id` inverse contract;
- removed semantic collision with Odoo core `res.partner.credit_limit` by using `ap_vendor_credit_limit`;
- migrated payment-term behavior to Odoo 19 `_compute_terms()` contract;
- migrated inventory valuation traceability from obsolete `stock.valuation.layer` / `stock_valuation_layer_ids` to native Odoo 19 `stock.move.value`;
- aligned payment lifecycle assumptions with Odoo 19;
- converted legacy SQL constraints to private `models.Constraint` class attributes;
- implemented Odoo 19 security privilege hierarchy;
- completed 9/9 Search/List/Form owner-model UI matrix;
- added governed AP lifecycle, approval, posting, payment registration and audit metadata;
- added runtime-safe/idempotent cross-addon Smart Button bridge;
- added regression tests, static guardrail, documentation and Windows targeted installer.

## Acceptance boundary
Source/static validation PASS does not claim runtime installation. Runtime remains pending until installed/activated on the user's Odoo 19 CE instance.

