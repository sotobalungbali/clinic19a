# clinic_ar Build — 2026-08-19

Version `19.0.3.0.2` is the authoritative runtime-repair candidate built from
the latest ClinicOne snapshot supplied on 19 Aug 2026.

Preservation rules:
- all active enterprise AR capabilities from 19.0.3.0.0 are preserved;
- basename-prefix-0 backup files are excluded;
- no downstream hard dependency on `clinic_ap` or `clinic_wallet` is added;
- Odoo 19 SQL objects remain private `models.Constraint` attributes;
- Codex remains a bounded implementation worker, never the architect.

Runtime repair #01 removes invalid `check_company=True` usage from the three
direct `res.currency` owner fields and adds guardrail/test coverage for this
Odoo 19 schema contract.

## Runtime Repair #02 — Billing Form Bridge
- Version: `19.0.3.0.2`
- Removed hard load-time `clinic_billing` form inheritance from AR XML.
- Added idempotent runtime-safe Billing Invoice/Payment Smart Button bridge.
- Current Billing Invoice parent supports the primary `button_box` candidate.
- Current Billing Payment parent has no `button_box`; the verified sheet fallback is used.
- Missing/incompatible parent layout is non-blocking for AR business installation.
