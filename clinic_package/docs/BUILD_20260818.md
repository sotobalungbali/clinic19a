# Full Corrected Build — 18 Aug 2026

This release re-opens the previously installed `clinic_package` baseline after `clinic_emar`
became a verified installed upstream dependency.

Root corrections:
1. preserve the already-proven package enterprise domain instead of rewriting it;
2. eliminate the same global `res.company` schema-drift blast-radius pattern previously exposed
   during eMAR development;
3. make the existing `clinic_emar` manifest dependency functionally meaningful through
   entitlement-to-medication traceability;
4. preserve cross-addon UI while keeping hard XML-ID drift non-fatal through runtime bridges;
5. add missing company rule for package tags;
6. strengthen migration, guardrail and regression contracts.

No package ownership was transferred to eMAR. No medication state transition is controlled by
`clinic_package`.
